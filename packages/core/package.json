{
  "name": "@builder6/core",
  "version": "0.0.1",
  "main": "dist/index.js",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -b",
    "build:watch": "rm -rf dist && tsc --watch"
  },
  "dependencies": {
  }
}
