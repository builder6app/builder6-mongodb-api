{
  "name": "@builder6/email",
  "version": "0.5.2",
  "main": "dist/index.js",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "build:watch": "rm -rf dist && tsc --watch"
  },
  "dependencies": {
    "@builder6/core": "^0.5.2",
    "nodemailer": "^6.9.16"
  }
}
