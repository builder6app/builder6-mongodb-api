{
  "name": "@builder6/email",
  "version": "0.0.1",
  "main": "dist/index.js",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "build:watch": "rm -rf dist && tsc --watch"
  },
  "dependencies": {
    "nodemailer": "^6.9.16",
    "@builder6/core": "^0.0.1"
  }
}
